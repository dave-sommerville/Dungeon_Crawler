namespace Dungeon_Crawler
{
    internal class Program
    {
        static void Main(string[] args)
        {/*
            Play play = new Play();
            play.PlayWithPet += IncreaseHappiness;
            play.PlayWithPet += DecreaseEnergy;
                    private static int PrintMenu()
        {
            int intDecision;
            while (true)
            {
                Console.Write("Select an action\n1) Feed Pet\n2) Play with pet\n3) Put pet to sleep");
                string decision = Console.ReadLine();

                if (int.TryParse(decision, out intDecision))
                {
                    if (intDecision >= 0 && intDecision <= 3)

                    {
                        return intDecision;
                    }
                    else
                    {
                        Console.WriteLine("Enter a number between 1 -3");
                    }
                }
                else
                {
                    Console.WriteLine("That is an invalid character.");
                }

            }
        }


            */
        }
        }
    }
    }
}
